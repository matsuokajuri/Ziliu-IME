# 安全策略

字流仍处于早期开发阶段，请不要在生产或敏感环境中安装未发布构建。

安全问题请通过仓库的私密安全报告渠道提交；在远程仓库建立前，可先保留最小复现、
版本号和受影响模块，不要在公开 issue 中粘贴真实输入内容。

项目的安全原则：

- 密码和敏感输入范围内不学习、不记录。
- TIP DLL 不联网。
- IPC 仅允许当前用户访问。
- 更新和安装产物必须签名。
- 日志默认不包含组合文本、已上屏文本、窗口标题或应用文档内容。

